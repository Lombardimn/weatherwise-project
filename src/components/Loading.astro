---

---

<!-- hidden or flex -->
<div class="loading hidden fixed inset-0 items-center justify-center z-50 bg-opacity-50 backdrop-blur-sm bg-gray-900" data-loading>
  <div 
    class="loader w-24 h-24 relative bg-white rounded-full rotate-45 animate-rotate before:content-[''] before:absolute before:left-1/2 before:top-1/2 before:w-[15px] before:h-[30px] before:bg-orange-500 after:content-[''] after:absolute after:left-1/2 after:top-1/2 after:w-[6px] after:h-[6px] after:rounded-full after:bg-white"></div>
</div>

<style>
  .loader:before {
    transform: skew(5deg , 60deg) translate(-50%, -5%);
  }

  .loader:after {
    transform:translate(-50% , -50%);
  }

  @keyframes rotate {
    0% { transform: rotate(45deg)}
    30% , 50% , 70%  { transform: rotate(230deg)}
    40% , 60% , 80% { transform: rotate(240deg)}
    100% {  transform: rotate(245deg)}
  }
</style>